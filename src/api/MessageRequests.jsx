// import axios from "axios";

// const API = axios.create({ baseURL: "http://127.0.0.1:9999" });

// export const getMessages = (id) => API.get(`/api/message/${id}`);

// export const addMessage = (data) => API.post("/api/message/", data);
